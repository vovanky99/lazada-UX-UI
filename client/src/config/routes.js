const routes = {
  home: '/',
  signIn: '/login',
  register: '/register',
  logout: '/logout',
  productdetails: '/products',
  search: '/search/:title',
  cart: '/cart',
  cat: '/cat',
};

export default routes;
