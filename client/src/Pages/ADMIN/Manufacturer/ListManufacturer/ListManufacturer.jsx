import classNames from 'classnames/bind';
import styles from '~/pages/ADMIN/Manufacturer';

const cx = classNames.bind(styles);

export default function ListManufacturer({ handleDelete = () => {} }) {
  return <></>;
}
