import classNames from 'classnames/bind';
import styles from '../Product.module.scss';

const cx = classNames.bind(styles);

export default function AddProduct() {
  return (
    <>
      <section>Add Shop</section>
    </>
  );
}
