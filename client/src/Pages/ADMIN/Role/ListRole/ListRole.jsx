import classNames from 'classnames/bind';
import styles from '~/pages/ADMIN/Role/Role.module.scss';

const cx = classNames.bind(styles);

export default function ListBlog({ handleDelete = () => {} }) {
  return <></>;
}
